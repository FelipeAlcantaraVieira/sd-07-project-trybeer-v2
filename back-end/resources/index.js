const user = require('./user');
const auth = require('./auth');
const product = require('./product');
const sales = require('./sales');
const messages = require('./message');

module.exports = { user, auth, product, sales, messages };
